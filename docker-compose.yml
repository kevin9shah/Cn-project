version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - API_KEY=changeme
      - DATABASE_URL=sqlite:////app/instance/network_health.db
    volumes:
      - .:/app
